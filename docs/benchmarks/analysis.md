# Van Roy baseline — analysis & hotspots

_Companion to [`baseline.md`](baseline.md), which is auto-regenerated by the
harness and only holds numbers. This file is hand-curated; the harness does
not touch it._

## 2026-07-21 re-measurement — the Phases 28–33 optimization arc, measured

Re-run after the Tier-1 speed arc (Phase 28), region compilation (Phase 29),
and the audit + cut/tail-call arc (Phase 33: ADR-027/028 indexing,
ADR-029/030/031 fusion + cut elision + CP-free guard commit, ADR-034 dynamic
inlining). 3 runs per cell; prior full table archived in
[`baseline-2026-06-04.md`](baseline-2026-06-04.md). Shumway per-iteration
time, June 4 → July 21:

| Benchmark | 2026-06-04 (µs) | 2026-07-21 (µs) | speed-up |
|---|---:|---:|---:|
| nreverse | 304 | 111 | **2.7×** |
| qsort | 599 | 115 | **5.2×** |
| queens | 1238 | 761 | 1.6× |
| tak | 54699 | 28458 | 1.9× |
| serialize | 142 | 75 | 1.9× |
| flatten | 195 | 52 | **3.7×** |
| sendmore | 740296 | 494489 | 1.5× |
| zebra | 8374 | 6010 | 1.4× |
| boyer | 16.5 | 16.2 | ~1.0× (already sub-noise) |
| crypt | 11964 | 3829 | **3.1×** |

Every benchmark improved 1.4–5.2× (geometric mean ≈ **2.2×**) except boyer,
whose per-iteration work was already below the noise floor. These are
same-machine, different-day numbers (±20–40 % thermal variance applies to any
single cell), but 2–5× is far beyond noise, and the cross-engine *ratios* —
measured back-to-back within one run — confirm it:

| Comparison | June geomean | **July geomean** | Notable |
|---|---:|---:|---|
| Shumway / GProlog native | ~13.6× slower | **~7.5×** slower | qsort 13.1→3.2×, zebra 6.0→4.0× |
| Shumway / SWI interpreted | ~3.0× slower | **~2.3×** slower | queens now **beats** SWI (0.86×); crypt at parity (1.07×) |

The June analysis below is retained as the historical hotspot list; several of
its targets were exactly what Phases 28–33 attacked.

## June improvement targets — status as of 2026-07-21 (Tier-1 + regions view)

The June list, re-audited against the current code and the T1 numbers. The
optimization target is now the **shipped Tier-1 region shape**, not the
interpreter:

1. **"Inline arithmetic in Tier-1 IL"** — ✅ **DONE** (ADR-018 both tiers,
   Phase 28's try/catch-free IL fast lanes, Phase 33 fusions). Evidence: the
   arithmetic-heavy trio shows the biggest T1 wins (tak 5.5×, crypt 4.5×,
   queens 3.3×).
2. **"`=..` specialisation"** — ✅ **DONE**: the current `Univ` builds the
   list directly in the heap with a single allocation (no AST↔heap round
   trip — the thing June criticized). Boyer runs 2.8× faster at T1.
3. **"Heap-cell allocation fast-path"** — **STILL VALID, now a Tier-1
   target**: region IL calls `AllocateHeap` / `AllocateHeapUnbound` as
   out-of-line engine methods (visible in `--dump-il`); inlining the
   bump-allocation (bounds check + top increment) into the emitted IL is the
   natural next lever for the allocation-bound shapes (nreverse T1 is ~1.5×
   off native GProlog; flatten ~3×).
4. **"Unification specialisation"** — **STILL VALID**: serialize is now the
   worst T1-vs-native ratio (~4×). Term construction + tree walking runs
   through generic cell unification helpers in IL.

New observations for the T1 backlog:
- **zebra** has the weakest T1 win (1.9×): pure structural generate-and-test
  whose choice points are *real* (no cut to elide) — the CP save/restore cost
  in IL is inherent to the search; ADR-026 already rejected variable-width
  CPs at ≤1 % ceiling. Likely little headroom short of smarter indexing.
- **Harness**: the external engines' startup subtraction uses a single spawn
  and poisoned cells on a loaded machine (one flatten-GProlog cell read
  0.45 µs/iter); worth hardening if cross-engine tables are re-generated
  regularly.

## Tier-1 (persisted region IL) — measured 2026-07-21

The harness gained a **Shumway T1** engine row: each benchmark compiled + linked
with `IncludeCompiledIl` (the linker's default **region** layout — what
`shumway-link --with-compiled-il` bakes into a bundle or `--exe`; runtime
promotion cannot produce regions), loaded via `LoadBundle` and timed warm
in-process, symmetric with the T0 cell.

**Definitive T0/T1 numbers** (interleaved min-of-5 in one process, equal N/10
warmup on both tiers — the repo's measurement discipline; the harness's first
median-based cells undersold T1, see the artifact note below):

| Benchmark | T0 µs/iter | T1 µs/iter | T1 speed-up |
|---|---:|---:|---:|
| tak | 36046 | 6550 | **5.5×** |
| nreverse | 63.8 | 13.5 | **4.7×** |
| crypt | 4918 | 1089 | **4.5×** |
| sendmore | 626778 | 168289 | 3.7× |
| queens | 648 | 197 | 3.3× |
| qsort | 124.2 | 41.8 | 3.0× |
| flatten | 48.7 | 17.0 | 2.9× |
| boyer | 13.2 | 4.7 | 2.8× |
| serialize | 70.3 | 28.7 | 2.5× |
| zebra | 5767 | 3121 | 1.9× |

**Every benchmark wins, 1.9–5.5×, geometric mean ≈ 3.3×.** Against
native-compiled GProlog this puts the shipped Tier-1 shape at roughly
**1–4× slower** by benchmark (vs the interpreter's ~7.5×): qsort ~1.2×,
nreverse ~1.5×, crypt ~1.6×, zebra/queens/sendmore/tak ~2–2.6×, flatten ~3×,
serialize ~4× (the worst remaining ratio).

**Measurement artifact, resolved.** The harness's first T1 cells reported
qsort at ~1.0× (no win) and one run showed boyer T1 "4.5× slower" — both
false. Each T1 measurement loads the persisted assembly into a fresh engine,
and **.NET's own tiered JIT re-compiles the region method mid-measurement**
when the warmup is too small (`bench(1)`); with a loaded machine the medians
landed on the un-tiered runs (stddev 50–90 % on those cells). Fix shipped in
the harness: the T1 warmup runs `bench(N/10)` so .NET tiering settles before
the stopwatch starts. Confirmed by IL inspection along the way: qsort's
predicates ARE region-compiled (root `qsort/3` absorbing `partition/4`), and a
cut-partition vs if-then-else-partition A/B showed the cut form is *faster*
under T1 (0.84×) — no codegen problem existed. Lesson (again): a low-stddev
cell inside a noisy run is not trustworthy; interleaved min-of-N is the
discipline.

---

## June 2026 analysis (historical)

_Based on the 2026-06-02 5-runs-per-cell measurement on
`GUSBRO-NB`, Intel 8-core, Windows 10 LTSC, .NET 10._

## Summary

| Comparison | Geometric mean of ratios | Best | Worst |
|---|---:|---|---|
| Shumway / GProlog native | **~13.6×** slower | zebra (8.5×) | serialize (25.5×) |
| Shumway / SWI interpreted | **~3.0×** slower | serialize (2.2×) | boyer (7.7×) |

GProlog and SWI themselves differ by ~5× in their favour, which is the
canonical interpretation-vs-native-compile gap. Shumway is currently
~3× behind SWI's bytecode interpreter — reasonable for a young project,
but with identifiable improvement targets below.

## Per-benchmark observations

### Strong cases for Shumway

- **`zebra` (8.5× vs gprolog)** — Shumway's closest match to native
  compile. Pure structural backtracking with no arithmetic. Our
  chunk-127+ in-place dynamic dispatch and trail machinery hold up
  well; the gap is the constant-factor cost of bytecode dispatch vs
  inlined machine code.

- **`serialize` (2.2× vs SWI)** — best ratio against SWI. Term
  construction + BST traversal exercises our heap GC (ADR-016) and
  unification path, both well-tuned.

### Hotspots — concrete leads

- **`serialize` (25.5× vs gprolog)** — the *worst* gap vs native,
  despite being our best vs SWI. The reading: gprolog's native compile
  of this exact pattern (heap allocation + tree walk) is just
  exceptionally good — it runs in 4.2 µs/iter, vs SWI's 48 µs (11×
  ratio between them). Not a Shumway-specific weakness, more a ceiling
  imposed by interpretation.

- **`crypt` (23.3× vs gprolog)** and **`queens` (21.5× vs gprolog)** —
  the two arithmetic-heavy workloads. gprolog compiles `is/2` to direct
  machine instructions; we dispatch through the bytecode interpreter's
  arithmetic evaluator + `Number` type. Inlining hot arithmetic in
  Tier-1 IL would close most of this gap — Phase 16's threaded IL
  dispatch already handles the call path, but the arithmetic
  evaluation itself is still bytecode-only.

- **`boyer` (7.7× vs SWI)** — the only benchmark above the SWI mean of
  ~3×. Boyer drives heavy `=../2`-based term reflection. SWI has a
  highly optimised C-level `=..`; our implementation does a full
  AST↔heap round-trip. Specialising `=..` for the common case
  (functor + args, known arity) is a clear win.

- **`tak` (14.4× vs gprolog, 3.7× vs SWI)** — the canonical
  recursion+arithmetic benchmark. Same story as queens/crypt — native
  arithmetic is hard to beat. Tail-call optimisation is fine on our
  side (chunk 110 confirmed LCO); the gap is purely per-call cost.

- **`nreverse` (13.1× vs gprolog, 2.8× vs SWI)** — heavy unification +
  cons-cell allocation. Allocation rate is the dominant factor; our
  heap GC (ADR-016) is competitive but not yet inline-optimised the
  way SWI's is for cons-cell hot paths.

## What this tells us about Shumway's invariants

- **Backtracking / cut machinery** is solid. zebra, sendmore, and
  queens all stay within the cluster's geometric mean. The chunk-118+
  in-place dynamic dispatch (ADR-015) is paying off.

- **Arithmetic** is the largest single deficit vs native gprolog.
  Three of the four worst ratios (queens, crypt, tak) are arithmetic-
  heavy. The bytecode evaluator was tuned for correctness, not for
  per-operation throughput.

- **`=..` and term reflection** are slow enough to dominate boyer.
  Specialised paths for common shapes would help.

- **No outlier-bad benchmark** vs SWI — every cell is within 2.2–7.7×.
  This means the engine doesn't have any "broken" workload; everything
  scales together with the dispatch-cost constant.

## What this is NOT yet

- No CPU-time measurement (only wall-clock) — process startup
  contaminates short workloads. We mitigate with the `bench(0)`
  startup subtraction, but for sub-millisecond per-iter benchmarks
  (boyer on gprolog) it bottoms out as `<noise>`.

- No memory tracking. Shumway is allocation-heavy in places; SWI uses
  a tagged-pointer scheme that avoids some of that. A future analysis
  pass should measure peak heap and GC frequency.

- One machine, one snapshot. Different CPUs (especially AMD vs Intel)
  shift the ratios non-trivially because gprolog's native code
  benefits from cache effects that the interpreted engines don't.

## Improvement targets, in priority order

1. **Inline arithmetic in Tier-1 IL** — `is/2`, `=:=/2`, `>/2` etc. with
   integer fast-paths. Targets crypt, queens, tak.
2. **`=..` specialisation** — fast path for `Term =.. [Functor|Args]`
   where Term is bound. Targets boyer.
3. **Heap-cell allocation fast-path** — inline cons-cell creation
   without going through the full ADR-016 GC barrier on the hot path.
   Targets nreverse, flatten, qsort.
4. **Unification specialisation** — list-pattern unification in Tier-1
   IL with no allocation for the result lists in deterministic cases.
   Targets serialize.

Each of these would close ~10–30% of the gap to SWI for its target
benchmarks. Closing the gap to native gprolog wholesale would require
a true machine-code backend, which is a much larger undertaking and
out of scope until at least Phase 27+.

## Reproducibility

Re-run with the same setup:

```
dotnet run -c Release --project tests/Shumway.Tests.Benchmarks/ -- --vanroy --runs 5
```

For quick spot-checks (drift from the baseline), `--runs 3` is enough
to wash out most noise and runs ~40% faster.
